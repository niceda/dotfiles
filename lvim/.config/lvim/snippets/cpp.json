{
  "StCode": {
    "prefix": "#",
    "body": [
      "#include <algorithm>",
      "#include <cmath>",
      "#include <iostream>",
      "#include <map>",
      "#include <queue>",
      "#include <set>",
      "#include <stack>",
      "#include <vector>",
      "using namespace std;",
      "",
      "int main() {",
      "\t$0",
      "  return 0;",
      "}"
    ]
  },
  "main": {
    "prefix": "main",
    "body": [
      "int main() {",
      "\t$0",
      "}"
    ]
  },
  "forc": {
    "prefix": "forc",
    "body": [
      "for (auto it = ${1:v}.begin(); it != $1.end(); it++) {",
      "\t$0",
      "}"
    ]
  },
  "forrc": {
    "prefix": "forrc",
    "body": [
      "for (auto it = ${1:v}.rbegin(); it != $1.rend(); it++) {",
      "\t$0",
      "}"
    ]
  }
}
